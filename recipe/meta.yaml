{% set name = "mecab-python3" %}
{% set version = "1.0.12" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 9aba1e56ef80dd97147dcbf8e3508147af129fb46cec0e432b85f96a9bdaa4b9

build:
  number: 0
  entry_points:
    - mecab-py = MeCab.cli:parse
    - mecab-py-info = MeCab.cli:info
  script: {{ PYTHON }} -m pip install . -vv
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
  host:
    - pip
    - python
    - setuptools_scm
    - setuptools
    - mecab
  run:
    - python
    - mecab

test:
  imports:
    - MeCab
  commands:
    - pip check
    - mecab-py-info
  requires:
    - pip
    - unidic-lite

about:
  home: https://github.com/SamuraiT/mecab-python3
  summary: Python wrapper for the MeCab morphological analyzer for Japanese
  license: GPL-2.0-only OR LGPL-2.1-only OR BSD-3-Clause
  license_file:
    - COPYING
    - GPL
    - LGPL
    - BSD

extra:
  recipe-maintainers:
    - rynorris
    - henry-thompson
